import{_ as e,o as i,c as s,ao as a}from"./chunks/framework.BrCix5Tt.js";const g=JSON.parse('{"title":"Construction","description":"","frontmatter":{},"headers":[],"relativePath":"construction.md","filePath":"construction.md","lastUpdated":null}'),n={name:"construction.md"};function o(l,t,h,d,r,c){return i(),s("div",null,[...t[0]||(t[0]=[a(`<h1 id="Construction" tabindex="-1">Construction <a class="header-anchor" href="#Construction" aria-label="Permalink to &quot;Construction {#Construction}&quot;">​</a></h1><p>Three blocked left-looking factorizations from Camarero, arXiv:1812.02056. Each forms one column at a time, defers the trailing update, and flushes it every <code>s</code> columns through a function you can replace.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> UpdatableFactorizations, LinearAlgebra</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">A </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> B </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> randn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">400</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">400</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Matrix</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Symmetric</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(B </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> B</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> +</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 400</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">I)) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">F </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cholesky_crout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(A; s </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># an UpdatableCholesky, ready to update</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">G </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> lu_crout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">randn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">400</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">400</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); s </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># partial pivoting, as LinearAlgebra.lu does</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">H </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> qr_bcgs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">randn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">600</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">400</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); s </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># an UpdatableQR, ready to update</span></span></code></pre></div><h2 id="Speed,-relative-to-LAPACK" tabindex="-1">Speed, relative to LAPACK <a class="header-anchor" href="#Speed,-relative-to-LAPACK" aria-label="Permalink to &quot;Speed, relative to LAPACK {#Speed,-relative-to-LAPACK}&quot;">​</a></h2><p>Every ratio below is candidate speed divided by baseline speed: <strong>above 1.00 is a win, below 1.00 is a loss.</strong> This is the opposite of the raw numbers stored in <code>bench/results/construction-neuromancer.json</code>, which record candidate time divided by baseline time — a ratio above 1.00 there means the candidate took <em>longer</em>. Figures here are single-BLAS- thread medians from that file.</p><table tabindex="0"><thead><tr><th style="text-align:right;">routine</th><th style="text-align:right;">flush / setting</th><th style="text-align:right;">baseline</th><th style="text-align:right;">n=2000</th><th style="text-align:right;">n=4000</th></tr></thead><tbody><tr><td style="text-align:right;"><code>cholesky_crout</code></td><td style="text-align:right;">symmetric rank-k, the default</td><td style="text-align:right;"><code>potrf</code></td><td style="text-align:right;">0.75 – 0.92</td><td style="text-align:right;">0.86 – 0.92</td></tr><tr><td style="text-align:right;"><code>cholesky_crout</code></td><td style="text-align:right;">full-block multiply</td><td style="text-align:right;"><code>potrf</code></td><td style="text-align:right;">0.51 – 0.56</td><td style="text-align:right;">0.51 – 0.52</td></tr><tr><td style="text-align:right;"><code>lu_crout</code></td><td style="text-align:right;">pivoted, the default</td><td style="text-align:right;"><code>getrf</code></td><td style="text-align:right;">0.79 – 0.90</td><td style="text-align:right;">0.83 – 0.89</td></tr><tr><td style="text-align:right;"><code>lu_crout</code></td><td style="text-align:right;">unpivoted</td><td style="text-align:right;"><code>getrf</code></td><td style="text-align:right;">0.87 – 0.97</td><td style="text-align:right;">0.89 – 0.96</td></tr><tr><td style="text-align:right;"><code>lu_crout</code></td><td style="text-align:right;">unpivoted</td><td style="text-align:right;">stdlib <code>lu!(A, NoPivot())</code></td><td style="text-align:right;">12.6× – 15.6×</td><td style="text-align:right;">15.1× – 16.4×</td></tr><tr><td style="text-align:right;"><code>qr_bcgs</code></td><td style="text-align:right;"><code>reorth = false</code></td><td style="text-align:right;"><code>geqrf</code> plus forming thin <code>Q</code></td><td style="text-align:right;"><strong>1.47 – 1.61</strong></td><td style="text-align:right;"><strong>1.38 – 1.62</strong></td></tr><tr><td style="text-align:right;"><code>qr_bcgs</code></td><td style="text-align:right;"><code>reorth = true</code>, the default</td><td style="text-align:right;"><code>geqrf</code> plus forming thin <code>Q</code></td><td style="text-align:right;">0.75 – 0.84</td><td style="text-align:right;">0.72 – 0.84</td></tr></tbody></table><p>Ranges span block sizes <code>s = 64, 128, 256</code> for Cholesky and LU and <code>s = 32, 64, 128, 256</code> for QR.</p><p><code>qr_bcgs</code> with <code>reorth = false</code> is the one routine here faster than LAPACK. It builds <code>R</code> from the projection coefficients the orthogonalization already produces, rather than closing with <code>Q&#39;*A</code>, which cost a further <code>2*m*n^2</code> and was 44% of the routine. <code>reorth = true</code>, the default, runs that projection a second time and is still slower than the baseline; see <a href="/UpdatableFactorizations.jl/dev/construction#construction-accuracy">Accuracy</a> for what the setting buys.</p><p>Every other cell loses to LAPACK. The unpivoted <code>lu_crout</code> figures against stdlib&#39;s own <code>NoPivot()</code> path are large only because that path is an unblocked generic fallback, not a LAPACK call — measured against <code>getrf</code>, which is what a caller actually gets from <code>LinearAlgebra.lu</code>, unpivoted <code>lu_crout</code> is 0.87 to 0.97, slightly slower like everything else in this table. Substituting a full-block multiply for Cholesky&#39;s default symmetric rank-k flush costs a further 1.7 to 2.0 times.</p><h2 id="construction-accuracy" tabindex="-1">Accuracy <a class="header-anchor" href="#construction-accuracy" aria-label="Permalink to &quot;Accuracy {#construction-accuracy}&quot;">​</a></h2><p><code>cholesky_crout</code> reaches <code>norm(L*L&#39; - A)/norm(A)</code> of 1.7e-16 to 2.4e-16 on a random SPD matrix, in <code>potrf</code>&#39;s own range (1.5e-16 to 1.8e-16 on the same matrices).</p><p><code>lu_crout</code>&#39;s accuracy depends heavily on which matrix it is measured against. On a diagonally dominant matrix, where every leading principal minor is nonzero and partial pivoting never swaps a row, unpivoted <code>lu_crout</code> reaches <code>norm(L*U - A)/norm(A)</code> of 1.7e-16 to 2.2e-16. That fixture says nothing about pivoted accuracy, because it never exercises a pivot.</p><p>On a plain random matrix, which swaps rows at essentially every column, <code>LinearAlgebra.lu</code> itself (via <code>getrf</code>) reaches a residual of 3.1e-15 at n=1000 and 3.7e-15 at n=2000, and pivoted <code>lu_crout</code> reaches 7.0e-15 and 1.4e-14 on the identical matrices — 2.3 and 3.7 times LAPACK&#39;s own residual, a ratio that grows slowly with <code>n</code>. Both are medians over five independent draws, whose spread is under two percent. The Crout elimination order differs from LAPACK&#39;s blocked right-looking one, and so does its growth factor.</p><p>Quoting the diagonally dominant figure next to LAPACK&#39;s pivoted one would suggest a hundredfold gap that is not there; the two numbers describe different matrices.</p><p><code>qr_bcgs</code> trades one residual for the other, not both at once:</p><table tabindex="0"><thead><tr><th style="text-align:right;">quantity</th><th style="text-align:right;"><code>geqrf</code> + form <code>Q</code></th><th style="text-align:right;"><code>qr_bcgs</code>, <code>reorth = false</code></th><th style="text-align:right;"><code>qr_bcgs</code>, <code>reorth = true</code></th></tr></thead><tbody><tr><td style="text-align:right;"><code>norm(Q*R - A)/norm(A)</code></td><td style="text-align:right;">1.2e-15</td><td style="text-align:right;">5.6e-16</td><td style="text-align:right;">6.1e-16</td></tr><tr><td style="text-align:right;"><code>norm(Q&#39;Q - I)</code></td><td style="text-align:right;">1.0e-13</td><td style="text-align:right;">1.7e-12</td><td style="text-align:right;">1.6e-12</td></tr></tbody></table><p>Reconstruction is <em>better</em> than the baseline&#39;s regardless of <code>reorth</code>, because <code>R</code> is built from the same coefficients that built <code>Q</code>: <code>Q*R</code> reassembles <code>A</code> from the pieces subtracted from it during elimination, an identity that holds however orthogonal the finished <code>Q</code> turns out to be. Orthogonality is what <code>reorth</code> controls. The table above cannot show it, because both settings land near 1.7e-12 on a well-conditioned random matrix; the settings separate as the condition number grows. <code>reorth = false</code> loses orthogonality as the square of the condition number, while <code>reorth = true</code> runs the projection a second time and holds it at order <code>eps</code> provided the condition number times <code>eps</code> stays well below one, at roughly twice the cost (see the speed table above). <code>qr_householder</code>, which calls <code>LinearAlgebra.qr</code> and wraps the result, is unconditionally more accurate on orthogonality than either setting.</p><h2 id="Non-BLAS-element-types" tabindex="-1">Non-BLAS element types <a class="header-anchor" href="#Non-BLAS-element-types" aria-label="Permalink to &quot;Non-BLAS element types {#Non-BLAS-element-types}&quot;">​</a></h2><p>The deferred flush only pays when it can hand a block to a BLAS kernel. Measured against the same routine run with <code>s &gt;= n</code>, which never flushes and so is the unblocked left-looking factorization:</p><table tabindex="0"><thead><tr><th style="text-align:right;">element type</th><th style="text-align:right;">n</th><th style="text-align:right;"><code>s = 16</code></th><th style="text-align:right;"><code>s = 64</code></th></tr></thead><tbody><tr><td style="text-align:right;"><code>BigFloat</code></td><td style="text-align:right;">120</td><td style="text-align:right;">0.48</td><td style="text-align:right;">0.69</td></tr><tr><td style="text-align:right;"><code>ForwardDiff.Dual</code></td><td style="text-align:right;">200</td><td style="text-align:right;">0.96</td><td style="text-align:right;">0.99</td></tr></tbody></table><p>Blocking a <code>BigFloat</code> factorization is a loss at both block sizes; for <code>ForwardDiff.Dual</code> it is roughly neutral. Neither element type is a BLAS type, so <code>rankk!</code>&#39;s default falls back to a full matrix multiply on both triangles instead of <code>syrk!</code>/<code>herk!</code>, and the panel bookkeeping the blocking adds has nothing to pay for it.</p><p>For <code>ForwardDiff.Dual</code> specifically, <code>LinearAlgebra.cholesky</code>&#39;s generic fallback is 3.12 times faster than this package&#39;s unblocked path. Use the standard library for <code>Dual</code>.</p><h2 id="Substituting-the-flush" tabindex="-1">Substituting the flush <a class="header-anchor" href="#Substituting-the-flush" aria-label="Permalink to &quot;Substituting the flush {#Substituting-the-flush}&quot;">​</a></h2><p><code>cholesky_crout</code> takes <code>rankk!(C, A, alpha, beta; uplo)</code> and computes <code>alpha*A*A&#39; + beta*C</code>. <code>lu_crout</code> and <code>qr_bcgs</code> take <code>matmul!(C, A, B, alpha, beta)</code> with the meaning of <code>LinearAlgebra.mul!</code>. Both are plain function-valued keyword arguments: there is no abstract type, no trait and no package extension.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">counted </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Ref</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mine!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(C, A, B, alpha, beta) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (counted[] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">mul!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(C, A, B, alpha, beta))</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">lu_crout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">randn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">200</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">200</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); s </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, matmul! </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mine!)</span></span></code></pre></div><p>The Cholesky default writes only one triangle, through <code>syrk!</code> for real element types and <code>herk!</code> for complex ones. Passing a full-block multiply instead is the <code>gemm</code> row of the speed table above.</p><h2 id="Failure" tabindex="-1">Failure <a class="header-anchor" href="#Failure" aria-label="Permalink to &quot;Failure {#Failure}&quot;">​</a></h2><p><code>cholesky_crout</code> throws <code>PosDefException(c)</code> at the first column with a non-positive pivot. <code>lu_crout</code> throws <code>ZeroPivotException(c)</code>; with <code>pivot = NoPivot()</code> that means the leading principal minor is singular, and <code>rtol</code> widens the test from exact zero to a threshold relative to the column&#39;s norm. <code>qr_bcgs</code> throws when a column&#39;s projection collapses onto the columns before it, which means the matrix is rank deficient; it takes the same <code>rtol</code>, and needs it, because a rank-deficient input in floating point leaves rounding noise rather than an exact zero.</p>`,28)])])}const k=e(n,[["render",o]]);export{g as __pageData,k as default};
